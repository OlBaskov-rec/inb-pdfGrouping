# PDF Grouping 0.1.26

### Изменено
- В области предупреждения о пересечении строки выровнены по столбцам (как таблица):
  диапазон, «пересекает», существующий диапазон, источник («текущие диапазоны») и
  «(повторяются …)» теперь стоят ровными колонками независимо от длины номеров.
- Кнопки в секции «Метка группы» снова прижаты к нижнему краю секции — вровень с нижней
  линией серой области «Диапазоны страниц» и «Сформированные группы»; расстояние между
  кнопками сохранено.
