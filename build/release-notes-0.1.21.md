# PDF Grouping 0.1.21

### Добавлено
- Иконка приложения (в заголовке окна, на панели задач и у exe).
- В разрешении конфликта пересечений появляется (с небольшой задержкой) кнопка
  «Оставить имеющиеся пересечения» — диапазоны не меняются, режим «Без пересечений»
  остаётся выключенным.

### Исправлено
- Кнопки в секции «Метка группы» больше не обрезаются снизу: секция растёт под
  содержимое, список выбранных диапазонов прокручивается при большом числе.

### Изменено
- Заголовок: «Решение конфликта по пересечению страниц выбранных диапазонов».
