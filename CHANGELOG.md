# Changelog

Все заметные изменения проекта документируются в этом файле.
Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/),
проект придерживается [семантического версионирования](https://semver.org/lang/ru/).

## [0.1.34] — 2026-06-25

**EN — Fixed:** "Check for updates" no longer hangs on "Checking…" (timeout added; broken in
0.1.31). **Changed:** window now remembers its size between launches. **Added:** regression
checklist `docs/CHECKLIST.md`.

**RU — Исправлено:** «Проверить обновление» больше не виснет на «Проверка…» (добавлен таймаут;
ломалось в 0.1.31). **Изменено:** окно запоминает размер между запусками. **Добавлено:**
регрессионный чек-лист `docs/CHECKLIST.md`.

## [0.1.33] — 2026-06-13

**EN — Changed:** "Current ranges" and "Created groups" lists are now height-limited (≈7 rows,
then a scrollbar), like "Page ranges" — panels no longer stretch down with many ranges/groups.
From now on notes are bilingual (English first, then Russian).

**RU — Изменено:** списки «Текущие диапазоны» и «Сформированные группы» ограничены по высоте
(≈7 строк, далее прокрутка), как «Диапазоны страниц» — секции не «улетают» вниз при большом
числе диапазонов/групп. С этого релиза заметки двуязычные (сначала англ., затем рус.).

## [0.1.32] — 2026-06-12

### Добавлено
- Повороты страницы в увеличенном просмотре: ↺ / ↻ на 90° (кнопки сверху по центру,
  только в увеличенном виде; на исходный PDF не влияют).

### Изменено
- Проверка обновлений запускается с задержкой после открытия окна, полностью в фоне.

## [0.1.31] — 2026-06-12

### Исправлено
- Возможное зависание при запуске: фоновая проверка обновления вынесена с UI-потока
  (пул потоков) и ограничена таймаутом 20 с — окно открывается сразу, UI всегда отзывчив.
  Ручная проверка/докачивание тоже не блокируют интерфейс.

## [0.1.30] — 2026-06-12

### Изменено
- Сценарий обновления: при авто-обнаружении баннер сразу не показывается — мигает значок «ℹ»;
  в меню появляется зелёная кнопка «Обнаружено обновление», по нажатию — скачивание и баннер
  с «Обновить и перезапустить». В баннере добавлена кнопка «Отложить».
- Тумблер «Без пересечений» блокируется при нерешённом вопросе по пересечению.

### Добавлено
- Авто-индекс имени выходного файла при совпадении («Имя (1).pdf» …) — существующие не перезаписываются.

## [0.1.29] — 2026-06-12

### Изменено
- Минимальная высота окна 660 → 480 (поддержка низких разрешений); окно подгоняется под
  рабочую область экрана, содержимое прокручивается, кнопки «Обработать»/«Без пересечений»
  всегда видны. Зум превью работает и по клику на изображении страницы (уже было).

## [0.1.28] — 2026-06-12

### Изменено
- Лаунчер portable назван как приложение — `PdfGrouping.exe` (раньше «PDF Grouping.exe» с
  пробелом → два похожих exe в папке). Отображаемое имя в программе — «PDF Grouping».

## [0.1.27] — 2026-06-11

### Добавлено
- Зелёная подсветка значка «ℹ О программе» (с миганием) при доступном обновлении на старте.

### Исправлено
- «Проверить обновление» не показывает ошибку блокировки файла, если обновление уже скачано
  фоновой проверкой — выводится дружелюбное сообщение, действие по зелёной кнопке.

## [0.1.26] — 2026-06-11

### Изменено
- Список в области предупреждения о пересечении выровнен по столбцам (SharedSizeGroup):
  ровные колонки для диапазонов, источника и «(повторяются …)» при любой длине номеров.
- Кнопки «Метка группы» прижаты к нижнему краю секции (вровень с серыми областями соседних
  секций); расстояние между кнопками сохранено.

## [0.1.25] — 2026-06-11

### Исправлено
- Проверка обновлений падала с «SSL connection could not be established» в сетях, где
  системный прокси перехватывает TLS. Обновления теперь ходят к GitHub напрямую (минуя
  прокси), с сохранением проверки настоящего сертификата.
- Сообщение об ошибке проверки обновлений показывает вложенные исключения.

## [0.1.24] — 2026-06-11

### Изменено
- Блокировка полей ввода страниц распространена и на «Решение конфликта» (раньше — только
  вопрос о пересечении).

### Исправлено
- При смене языка во время активного предупреждения/конфликта переводятся и динамические
  части сообщения (диапазоны, «повторяются …», предлагаемые диапазоны, запрос объединения) —
  без иноязычных вкраплений.

## [0.1.23] — 2026-06-11

### Изменено
- Поля ввода номеров страниц блокируются, пока не принято решение по пересекающемуся
  диапазону (кнопки в баннере) — нельзя изменить числа при незавершённом вопросе.

## [0.1.22] — 2026-06-11

### Добавлено
- Многоязычный интерфейс (9 языков): русский, английский, французский, испанский,
  немецкий, итальянский, китайский, японский, корейский — переведены все надписи и сообщения.
- Кнопка выбора языка вверху: код текущего языка + меню; переключение «на лету», выбор
  сохраняется (в %AppData%/PdfGrouping/settings.json). Стартовый язык — по системе, иначе русский.

### Изменено
- Кнопка «О программе» — только значок «ℹ».

## [0.1.21] — 2026-06-11

### Добавлено
- Иконка приложения (заголовок окна, панель задач, exe).
- Кнопка «Оставить имеющиеся пересечения» в разрешении конфликта (появляется с задержкой):
  диапазоны не меняются, режим «Без пересечений» остаётся выключенным.

### Исправлено
- Кнопки в секции «Метка группы» не обрезаются (секция растёт под содержимое, список
  диапазонов прокручивается).

### Изменено
- «Решение конфликта по пересечению страниц выбранных диапазонов».

## [0.1.20] — 2026-06-11

### Исправлено
- Тумблер «Без пересечений» не остаётся «нажатым» при включении во время неразрешённого
  вопроса: режим не активируется, показывается подсказка, кнопка возвращается в выкл.
  (откат тумблера отложен через диспетчер — иначе ToggleButton не сбрасывал вид).

## [0.1.19] — 2026-06-10

### Изменено
- Поле «Сохранить результаты в:» закреплено внизу окна (вместе с кнопками) — не пропадает
  при уменьшении окна.
- Убрано двоеточие: «(повторяются страницы …)».
- Подсказка про «Без пересечений» при активном вопросе — цветной текст с отступом
  («Режим 🚫 Без пересечений не активирован, т.к. не решён текущий вопрос…»).

## [0.1.18] — 2026-06-10

### Исправлено
- Кнопки «Обработать»/«Без пересечений» закреплены внизу окна (вне прокрутки) — при
  уменьшении окна не пропадают.
- Восстановлен показ блок-сообщений в области уведомлений (раньше гасились сразу).

### Изменено
- Единый размер всех заголовков в области уведомлений (Внимание / Без пересечения / Решение конфликта).
- После «Добавить ещё раз» кнопки решения скрываются (предупреждение остаётся как информация).
- Включение «Без пересечений» при активном вопросе по диапазону не открывает второй вопрос,
  а выводит сообщение «Сначала примите решение по добавлению диапазона».
- Увеличены отступы у кнопок «Добавить диапазон» / «…постранично» (до и между).

## [0.1.17] — 2026-06-10

### Изменено
- В предупреждении о пересечении: «дубли:» → «повторяются:».
- Строка «Без пересечения добавится: …» — отступы сверху/снизу и чуть крупнее.
- Кнопки «Метка группы» переименованы: «Создать группу для вывода» и
  «Создать группу для вывода по каждому диапазону».
- Эти кнопки перенесены в низ секции (списку диапазонов больше места); отступы вокруг
  кнопок увеличены в 1.5 раза.

## [0.1.16] — 2026-06-10

### Исправлено
- Отключение предпросмотра больше не оставляет окно шире, чем было (ширина запоминается
  при включении панели и восстанавливается при выключении).

### Изменено
- Убрана строка сообщений рядом с кнопками «Обработать» / «Без пересечений»: рутинные
  подтверждения больше не выводятся, ошибки показываются в общей области уведомлений (баннер).

## [0.1.15] — 2026-06-10

### Исправлено
- При включённом предпросмотре секция «Диапазоны страниц» больше не наезжает на
  «Метку группы» (минимальные ширины колонок + минимальная ширина окна с учётом панели).
- Включение предпросмотра больше не растягивает окно вниз — панель прокручивается,
  высота окна не меняется.

## [0.1.14] — 2026-06-10

### Исправлено
- Поля ввода страниц больше не «вытягиваются» и не выдают ошибку при временном
  очищении поля (значения стали `decimal?` с проверкой на пустоту).

### Изменено
- Поля номеров страниц получили фиксированную ширину под 4 символа (со стрелками ↑/↓).
- Кнопки «Очистить» (слева) и «👁 Предпросмотр» (справа) разнесены по краям секции.
- Окно больше не растёт вниз при появлении предупреждений — все сообщения выводятся
  в области уведомлений.

## [0.1.13] — 2026-06-05

### Добавлено
- Кнопка «Добавить без пересечения» в предупреждении (обрезает диапазон до свободных страниц)
  с предпросмотром добавляемых страниц.
- Автоподстановка метки «A», если ещё нет ни одной группы.
- Поля ввода страниц: отдельная строка, крупнее, со стрелками ↑/↓ (NumericUpDown).

## [0.1.12] — 2026-06-05

### Изменено
- Пересекающийся диапазон не добавляется без решения пользователя (Добавить ещё раз / Убрать);
  до решения другие добавления заблокированы.
- В режиме «Без пересечений» требуется решить конфликт перед продолжением.
- Выбор нового PDF обнуляет всю предыдущую сессию.
- Отступы кнопок в «Диапазоны страниц»; стрелка ► у «Создать группу на каждый диапазон».

## [0.1.11] — 2026-06-05

### Добавлено
- «+ Добавить диапазон постранично» — выбранные страницы → отдельные 1-страничные диапазоны.
- «Создать группу на каждый диапазон» — каждый диапазон становится отдельной группой.

### Изменено
- Сообщение «Пересечения запрещены» выводится в общей области уведомлений (баннере).

## [0.1.10] — 2026-06-05

### Добавлено
- Разрешение конфликта пересечений: при включении «Без пересечений» с уже имеющимися
  пересечениями — блок предложения непересекающихся диапазонов (обрезка/разбиение,
  приоритет занявшего раньше) с кнопками «Подтвердить» / «Убрать пересекающиеся».
  Алгоритм `PageRangeUtils.ResolveOverlaps` (+тесты).

## [0.1.9] — 2026-06-05

### Изменено
- Баннер предупреждения о пересечении не скрывается после «Добавить ещё раз».
- «Метка группы»: подпись «(любые 15 симв.)» под заголовком; 5 кнопок (A–E) в один ряд;
  «Текущие диапазоны» — под кнопками.

## [0.1.8] — 2026-06-05

### Добавлено
- Кнопки быстрого выбора метки группы (A–F).
- Объединение в существующую группу: при вводе её имени — вопрос «Добавить диапазоны в неё?».
- Метка группы: ограничение 15 символов и проверка на недопустимые в именах файлов символы.

### Изменено
- Увеличен зазор справа под полосу прокрутки.
- К дублируемым страницам добавлены единицы («страница 10» / «страницы 70–90»).
- Убран значок ⚠ у кнопок (предупреждения только в баннере).
- «Текущие диапазоны» — по одному на строке и крупнее.
- В режиме «Без пересечений» запрет распространён на создание/объединение групп.

## [0.1.7] — 2026-06-05

### Изменено
- Зазор справа под вертикальную полосу прокрутки (не наезжает на блоки).
- «Диапазоны страниц»: 3 строки по умолчанию, рост блока и колонок при 4–5 строках,
  прокрутка с 6-й; отступ до полосы прокрутки внутри списка.
- Убрано дублирующее сообщение о пересечении у кнопок «Обработать»/«Без пересечений».
- Версия приложения синхронизирована с версией сборки (csproj).

## [0.1.6] — 2026-06-05

### Изменено
- Единое место для предупреждений о пересечении — красный баннер (включая пересечения
  внутри текущей группы); дублирующее сообщение из статусной строки убрано.
- Шрифт баннера уменьшен; итоговый список дублируемых страниц — диапазонами (компактно).

## [0.1.5] — 2026-06-05

### Изменено
- Предупреждение о пересечении в статусной строке отмечается заметным красным значком ⚠
  (вместо серого символа) уже при выборе диапазонов.

## [0.1.4] — 2026-06-05

### Изменено
- Панель предпросмотра: заголовок и миниатюры по центру; между начальной и конечной
  страницей — разделитель с информацией о диапазоне (страниц, «от → до»).
- Слоган в «О программе» переведён на английский.
- Уточнён текст предупреждения о пересечении (добавлено «Диапазоном» и номера дублей).

## [0.1.3] — 2026-06-04

### Изменено
- В предупреждении о пересечении перечисляются конкретные номера дублируемых страниц.
- Увеличены минимальные размеры окна (динамические с учётом панели предпросмотра),
  чтобы интерфейс не сжимался внахлёст.

## [0.1.2] — 2026-06-04

### Добавлено
- Сводка «Дублирующиеся страницы: …» в предупреждении о пересечении (компактный список).
- Тумблер «Без пересечений» у кнопки «Обработать» — запрет добавления пересекающихся диапазонов.

### Исправлено
- Падение при изменении размера окна с показанным предупреждением (цикл привязки).
- Наложение элементов при уменьшении окна (минимальная ширина, динамическая с предпросмотром).

## [0.1.1] — 2026-06-04

### Добавлено
- Предпросмотр миниатюр начальной/конечной страниц диапазона («👁 Предпросмотр») с увеличением («🔍»).
- Окно «ℹ О программе» с версией, авторством и ручной проверкой обновлений.
- Версия программы в заголовке окна.

### Изменено
- Предупреждение о пересечении диапазонов с прежними группами: крупный значок ⚠, каждое
  пересечение отдельной строкой, окно расширяется вниз; кнопки «Добавить ещё раз» / «Убрать».
- Адаптивная компоновка: прокрутка и перенос кнопок вместо наложения при уменьшении окна.

## [0.1.0] — 2026-06-02

Первый релиз.

### Возможности
- Открытие PDF через кнопку «Обзор…» или **drag & drop** в окно.
- Деление документа на **группы по диапазонам страниц** с произвольными метками
  (буква/номер/слово); несколько несвязных диапазонов в одной группе.
- Склейка каждой группы в отдельный PDF-файл одним нажатием «Обработать».
- Список созданных файлов и кнопка «Открыть папку» с результатами.
- Кросс-платформенный интерфейс (Windows; macOS — в планах).

### Технологии
- UI: **Avalonia 12** (.NET 10).
- Работа с PDF: **PdfSharp 6** — без внешних утилит (qpdf не требуется).
- Авто-обновление: **Velopack** (фид — GitHub Releases).
- Все зависимости под свободными лицензиями MIT/Apache-2.0 (без AGPL).

### Известные ограничения
- Сборка пока без цифровой подписи — при первом запуске возможно предупреждение SmartScreen.
- Зашифрованные/повреждённые PDF могут не открыться (выводится понятная ошибка).

[0.1.0]: https://github.com/OlBaskov-rec/inb-pdfGrouping/releases/tag/v0.1.0
